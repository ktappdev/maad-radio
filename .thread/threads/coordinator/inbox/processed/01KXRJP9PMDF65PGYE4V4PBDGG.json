{
  "id": "explorer/01KXRJP9PMDF65PGYE4V4PBDGG",
  "from": "explorer",
  "to": "coordinator",
  "body": "## Root Cause Hypothesis: Facebook Graph API v19.0 Deprecated\n\n**Likeliest cause: Hardcoded API version v19.0 (Dec 2022) — almost certainly sunset by July 2026.** Facebook sunsets API versions ~2 years after release. v19.0 would have gone dark ~Dec 2024. The API calls now return errors, `live_videos` endpoint responds with empty data or errors, component gets no `embed_html`, and the iframe never renders.\n\n---\n\n### File: Line References & Findings\n\n**1. API Route — `src/app/api/live-video/route.ts`**\n\n- **Line 18:** `https://graph.facebook.com/v19.0/${PAGE_ID}/live_videos` — Hardcoded v19.0, introduced in commit `09021b6` (Jan 20, 2026). By this date v19.0 was already >3 years old. **Root cause.**\n- **Line 5:** `PAGE_ID = \"maad975\"` — vanity username, not numeric page ID. Works with valid token but adds fragility.\n- **Line 8-9:** Falls through `FACEBOOK_ACCESS_TOKEN` or `NEXT_PUBLIC_FACEBOOK_ACCESS_TOKEN`. If neither is set in production env vars → `route.ts:11` returns 500 with `\"Missing Facebook access token on server\"`.\n- **Line 22:** `broadcast_status: '[\"LIVE\"]'` — added in commit `e4768df`. Might be invalid param format. Facebook Graph API may not support this as a query filter on the `live_videos` endpoint, or expect different encoding.\n- **Lines 32-40:** Error relay returns `data?.error?.message` but the client (`GetLiveVideo.tsx`) **never reads error details** — it silently catches and clears state. The error path is opaque to the user.\n\n**2. Client Component — `src/app/components/GetLiveVideo.tsx`**\n\n- **Line 3:** `import { isMobile } from \"react-device-detect\"` — correct named import.\n- **Line 17-19:** `useEffect` returns early on mobile → no fetch → `isLive` stays `false`. **Bug: the `if (isMobile && isLive)` branch (line ~41) can never render on mobile.**\n- **Line 29-30:** Builds `embedUrl` by regex-parsing Facebook's `embed_html` string: `src=\"([^\"]+)\"`. Fragile — if Facebook changes embed HTML format (e.g., single quotes, data-src, new attributes), the regex returns empty string and video doesn't render.\n- **Line 48:** `console.error` statement exists but error goes to console only — no user-facing error state.\n- **Lines 81-120 vs 122-146:** Desktop and mobile JSX are **identical duplicates**. Redundant code, doubles maintenance surface.\n\n**3. Security Headers — `next.config.js`**\n\n- **Line 14:** `X-Frame-Options: DENY` — prevents THIS site from being framed externally. Does NOT block embedding Facebook's iframe ON this site. Not the issue.\n- **No `Content-Security-Policy` header** — so no CSP blocking iframes. Not the issue.\n\n**4. Environment — `.env.example`**\n\n- Only contains `NEXT_PUBLIC_DOMAIN`. No Facebook access token documented anywhere in repo. No `.env.local` file exists. Token must be configured only in production deployment vars — if expired or missing, API returns 500 silently.\n\n**5. Git History**\n\n| Commit | Change |\n|---|---|\n| `09021b6` (Jan 2026) | Created API route with v19.0 |\n| `e4768df` (Jan 2026) | Added `broadcast_status` filter |\n| `a02ffe0` (Jan 2026) | Aspect ratio fix on iframe wrapper |\n| `385484e` (Feb 2026) | Removed \"no video\" fallback div |\n\nThe `IsMobile` issue and duplicate JSX are pre-existing, not regressions.\n\n---\n\n### Embed Mechanism\n\n**Type:** OEmbed via Facebook Video Plugin iframe. The API returns `embed_html` containing:\n```html\n<iframe src=\"https://www.facebook.com/plugins/video.php?href=...\" ...></iframe>\n```\n**No SDK** loaded — pure iframe embed. No Facebook JS SDK initialization happening anywhere (confirmed by checking layout.tsx — only structured data script and YouTube preconnect link).\n\n---\n\n### Priority for Fix\n\n1. **Update Graph API version** — v19.0 → latest (v22.0 as of mid-2026). Check `live_videos` endpoint still exists in newer versions.\n2. **Verify access token** — ensure valid non-expired token in production env vars.\n3. **Error handling** — client should show user-facing message on API failure, not just console.error.\n4. **Simplification** — deduplicate identical mobile/desktop JSX branches.\n5. **Validate `broadcast_status` param** — confirm Facebook actually accepts it on `/{page-id}/live_videos` endpoint.",
  "sentAt": "2026-07-17T17:41:21.237Z",
  "re": "coordinator/01KXRJKPVT038T3WDJMW5F2M6S"
}