{
  "id": "reviewer/01KXRK2MGQS1CBV8FP3PCZ0RKT",
  "from": "reviewer",
  "to": "coordinator",
  "body": "## Review Complete\n\n**Verdict: request-changes**\n\nDiff is technically clean — dedup correct, no dropped behavior, mobile path now works, error state wired properly, lint/tsc green. But **root cause of \"video don't load anymore\" not addressed.**\n\n---\n\n## Critical (must fix)\n\n**1. `GetLiveVideo.tsx:26-28` — Silent failure when API returns 200 but no embed_html**\n\n```typescript\nconst isCurrentlyLive =\n  first &&\n  (first.status === \"LIVE\" || first.status === \"LIVE_NOW\") &&\n  first.embed_html;\n```\n\nIf API returns `data: []` or video without `embed_html`, `isCurrentlyLive` → false, `error` stays null. Component renders nothing. No feedback to user. **This is the most likely actual root cause** — the empty-response path is completely silent.\n\nFix: When `hasChecked && !isLive && !isLoading && !error`, render a fallback (e.g. \"No live stream right now\").\n\n**2. `route.ts:22` — `broadcast_status` param format suspect**\n\n```typescript\nurl.searchParams.set(\"broadcast_status\", '[\"LIVE\"]');\n```\n\n`URLSearchParams.set` produces `broadcast_status=%5B%22LIVE%22%5D`. Facebook docs say JSON array is valid for `live_videos` edge, but some API versions expect plain string `LIVE`. If this filter returns empty when it shouldn't → empty `embed_html` → hits issue #1 silently.\n\nFix: Test with plain `\"LIVE\"` as fallback, or remove filter and filter client-side.\n\n**3. `GetLiveVideo.tsx:88` — Broken link when `videoLink` empty**\n\n```tsx\n<a href={videoLink} ...>\n```\n\n`videoLink` initialized to `\"\"`. If `embed_html` lacks `href` param → `<a href=\"\">` self-referencing link. Should guard: `{videoLink && (<a ...>)}`.\n\n---\n\n## Warnings\n\n**4. `route.ts:10-11` — Public token fallback exposed to client bundle.** `NEXT_PUBLIC_FACEBOOK_ACCESS_TOKEN` prefix means it's in the JS bundle. Also no scope validation — expired/limited token → Graph API may return 200 with empty data.\n\n---\n\n## Suggestions\n\n**5. `GetLiveVideo.tsx:72-79`** — Consider rendering \"We're not live right now\" when `hasChecked && !isLive && !isLoading && !error`. Currently just blank.\n\n**6. `route.ts:51`** — Logging video count every 30s poll. Harmless but watch log volume.\n\n---\n\n## Discrepancy Note\n\nCoordinator stated builder \"made NO changes to route.ts.\" Git diff shows v19.0→v24.0 plus logging additions ARE part of this diff. Committed version was v19.0; explorer had read working tree (already edited by builder).\n\n---\n\n## Summary\n\nBuilder's changes are safe and improve error handling for network/exception failures. But the real root cause is likely a **silent empty-response path**: `broadcast_status` filter returning no matches, or valid response with missing `embed_html` — neither shows any UI feedback. Fix #1 and #2 together to close the gap.",
  "sentAt": "2026-07-17T17:48:05.530Z",
  "re": "coordinator/01KXRJYPZTDQXS320DF3X2GDM6"
}