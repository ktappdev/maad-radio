/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["source.unsplash.com", "i.pravatar.cc", "upload.wikimedia.org", "developer.apple.com"],
  },
};

module.exports = nextConfig;
